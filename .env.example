# --- PostgreSQL (obligatorio en Coolify) ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=cambia_esta_password_segura
POSTGRES_DB=exi_db

# --- App ---
APP_NAME=EXI API
DEBUG=false
CORS_ORIGINS=*

# --- SMTP (opcional; vacío = guarda emails en outbox del contenedor) ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_TLS=true
SMTP_SSL=false
