# Deploy Coolify — exitimv2

Igual que la versión anterior (tablas-app / baseapps):

1. New Resource → **Application** (no Docker Compose)
2. Repo: `hectordsc-4/exitimv2` (branch `main`)
3. Build Pack: **Dockerfile**
4. Port: **8000**
5. Environment Variables → pega el contenido de `deploy/coolify.env`
6. Dominio + Deploy

Login: `admin` / `admin123`
